<h1>$venue.getName()</h1>

<h3>A(n) $venue.getMusic() venue</h3>

<h3>Bands who have played here:</h3>
<dl>
#foreach($band in $bands)
  <dt>$band.getName()</dt>
  <dd>Plays $band.getMusic()</dd>
#end
</dl>
